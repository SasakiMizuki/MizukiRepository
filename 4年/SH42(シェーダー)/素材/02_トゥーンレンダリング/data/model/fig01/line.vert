//------------------------------------------------------------
// line

//------------------------------------------------------------
//頂点シェーダ
//------------------------------------------------------------
void main( void )
{
	//座標変換
	gl_Position	= gl_ModelViewProjectionMatrix * ( gl_Vertex + vec4( vec3( 0.1 ) * gl_Normal, 0.0 ) );
}
